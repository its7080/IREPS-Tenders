# Common Commands

## Windows menu runner

```bat
run_menu.bat
```

Use option `1` to create/update the `venv` virtual environment and install the unified root `requirements.txt`.

## Manual setup

```bat
py -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
```
